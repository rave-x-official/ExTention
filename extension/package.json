{
  "name": "malware-phishing-shield",
  "version": "1.0.0",
  "private": true,
  "license": "GPL-3.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch & esbuild src/background.ts --bundle --outdir=dist --format=esm --outbase=src --watch",
    "build": "npm run build:popup && npm run build:scripts",
    "build:popup": "vite build",
    "build:scripts": "esbuild src/background.ts --bundle --outdir=dist --format=esm --outbase=src"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
